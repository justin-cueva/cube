import { Fragment } from "react";

const SpinButtons = () => {
  return (
    <Fragment>
      <button className="spin-btn top">top</button>
      <button className="spin-btn right">right</button>
      <button className="spin-btn bottom">bottom</button>
      <button className="spin-btn left">left</button>
    </Fragment>
  );
};

export default SpinButtons;
